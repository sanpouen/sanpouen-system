/* eslint-disable no-unused-vars */
import * as React from 'react';
import CalendarTemplates from '../../templates/calendarTemplates';

const Calendar = () => {
    return(
        <CalendarTemplates />
    );
};

export default Calendar;